[package]
name = "deploy-toolkit-zed"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Zed host helpers for deploy-toolkit binary and LSP version checks."

[dependencies]
deploy-toolkit-host = { path = "../deploy-toolkit-host" }

[dev-dependencies]
serde_json = { workspace = true }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
