name: shipwright
description: Generic binary resolver + --version contract helpers for shipwright-style products.
version: 0.1.0
homepage: https://github.com/Nimblesite/Shipwright
repository: https://github.com/Nimblesite/Shipwright

environment:
  sdk: ^3.4.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
