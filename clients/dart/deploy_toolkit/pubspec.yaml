name: deploy_toolkit
description: Generic binary resolver + --version contract helpers for deploy-toolkit-style products.
version: 0.1.0
homepage: https://github.com/nimblesite/deployment_toolkit
repository: https://github.com/nimblesite/deployment_toolkit

environment:
  sdk: ^3.4.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
