{
  "name": "deploy-toolkit",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI checks for deploy-toolkit manifests, binaries, and extension packages.",
  "bin": {
    "deploy-toolkit": "./index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "yauzl": "^3.2.0"
  }
}
