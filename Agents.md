<!-- agent-pmo:f481f8d -->
@CLAUDE.md

Spec, plan, ticket, and coverage IDs MUST be hierarchical and non-numeric.
Use semantic words, such as `DTK-MIG-DESLOP-IDE` or `DTK-SPEC-BINARY-VERSION`.
Do not create sequential numeric IDs such as `DTK-MIG-003`, `DTK-SPEC-001`, or `[SPEC-001]`.
